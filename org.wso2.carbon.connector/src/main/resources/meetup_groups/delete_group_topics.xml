<template name="delete_group_topics" xmlns="http://ws.apache.org/ns/synapse">

    <!-- Required Parameters -->
    <parameter name="key" description="API key" />
    <parameter name="urlname"  />
    <parameter name="topic_id"  />




    <sequence>
        <property name="uri.var.key" expression="$func:key" />
        <property name="uri.var.urlname" expression="$func:urlname" />
        <property name="uri.var.topic_id" expression="$func:topic_id" />






        <call>

            <endpoint>

                <http method="delete"
                      uri-template="https://api.meetup.com/{uri.var.urlname}/topics?topic_id={uri.var.topic_id}&amp;key={uri.var.key}" />

            </endpoint>
        </call>
        <!-- Remove response custom header information -->
        <header name="x-li-format" scope="transport" action="remove" />
        <header name="X-LI-UUID" scope="transport" action="remove" />
        <header name="X-Li-Pop" scope="transport" action="remove" />
        <header name="X-Li-Fabric" scope="transport" action="remove" />
        <header name="x-li-request-id" scope="transport" action="remove" />
    </sequence>
</template>